package com.pause.build

// Convention plugins are defined in buildSrc but currently unused.
// Modules apply plugins directly via root build.gradle.kts version constraints.
// This file is reserved for future convention plugin implementations.
